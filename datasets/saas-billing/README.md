# Dimensional Export — SaaS Billing

Generated by fabulexa-forge; do not edit by hand. See `saas-billing-dimensional-manifest.json` for this export's machine-readable companion manifest.

## Overview

The billing scenario for the inherited-complex-query teaching track. The
warehouse carries every billing **fact** the simulation emits — plan terms,
the per-SKU rate ladder, negotiated commercial terms per account, promotions,
credits, seats and usage — and none of the **outcomes**: there is no invoice
table, no commit balance, no applied-credit residue. Those are the reference
query's job, and the spec below is what that query implements.

### Billing spec — the reference query's stage order

One invoice per account per calendar month. Stages run in this order; each
consumes the previous stage's output.

1. **Billable seats** — seats whose `dim_user_status` interval overlaps the
   month in a billable state (`active` or `using`), prorated by seat-days
   over the month's days. `provisioned` and `offboarded` days are not billed.
2. **Seat charge** — billable seats × `dim_account_terms.seat_price` as of
   month end.
3. **Usage per account / SKU / month** — the `volume` leaf of
   `fact_usage_event.context` (a JSON payload: `json_extract(context,
   '$.volume')`) summed by the SKU each session bound; only
   `billing_model = metered` SKUs rate.
4. **Promotions** — volume on a SKU during an active `dim_promotion` window
   (`starts_at` ≤ `occurred_at` < `ends_at`) is removed before anything
   else, as is all volume inside the account's free trial (`dim_account.
   created_at` + `dim_plan_terms.trial_days`).
5. **Allowance** — Core (`SKU-001`) volume is netted by billable seats ×
   included units: `dim_account_terms.allowance_override` when present,
   else `dim_plan_terms.included_units_per_seat` for the account's plan.
6. **Tiered rating** — the remaining cumulative period volume is walked up
   the ladder band by band. The ladder is `custom_tier` for the account +
   SKU when a row is effective for the month (see that table's notes),
   otherwise `dim_rate_tier` restricted to rows effective at month end. A
   metered SKU with no ladder at all (`Workflow`, `SKU-007`) rates at
   `dim_sku.standard_rate` flat.
7. **Negotiated discount** — the rated amount × `dim_account_terms.
   pct_of_list_rate / 100` as of month end. A `custom_tier` row that
   carries `pct_of_list` instead of `rate` *replaces* this stage for that
   band: it is applied to `dim_sku.standard_rate`, and the account's own
   `pct_of_list_rate` is not applied on top.
8. **Flat add-ons** — `dim_sku.flat_fee` for each `flat` SKU the account
   subscribes to (`dim_account.addon_sso` / `addon_support`).
9. **Commit drawdown** — for an enterprise account with `term_commit` > 0,
   usage charges draw the commit down across the term's months (a term is
   the interval between consecutive `dim_account_terms` versions whose
   `term_commit` changed); charges above the commit bill as overage; the
   unused balance expires at term end.
10. **Credits** — open `fact_credit` rows (issued before month end, not yet
    expired: `issued_at` + `dim_plan_terms.credit_validity_days`) apply
    FIFO by `issued_at`; a partially consumed credit carries its remainder
    forward. Credits reduce the invoice, never below zero.

**Conventions.** Month-end attribute values (stage 2, 5, 7) are the
`dim_account_terms` version whose `[valid_from, valid_to)` contains the
last instant of the month. The month an instant belongs to is read through
its `*_date_key` on `dim_date` (the local date in the export's zone), and a
month's length is the calendar's row count for that month. Volume is in the
SKU's own units; the ladder bounds are inclusive on both ends.

### Deliberate ambiguities

The spec above picks one reading. The alternatives are real, and a reviewer
of the inherited query should be able to say which one it implements.

- **Price as of when?** — month end (above) vs the instant of each usage
  event. They differ whenever terms change mid-month.
- **Band bounds and the repriced Analytics band** — `dim_rate_tier` has two
  rows for Analytics band 2, one deactivated on the day the other starts.
  "Effective at month end" and "effective at event time" give different
  rates for the changeover month.
- **Allowance before or after promotion** — the spec removes promo volume
  first; netting the allowance first is the common mistake.
- **Commit terms** — read off `term_commit` version boundaries (988
  changes across the run) or off the review cadence itself
  (`fact_lifecycle_interval` rows in state `reviewed`, 2,977 of them); the
  two disagree whenever a review leaves the commit unchanged.

### Known traps

- The metered quantity is not a column. It lives inside the JSON
  `fact_usage_event.context` as `volume`; summing anything else sums
  nothing.
- Renewal reviews never touch `dim_company.status` (onboarding / active /
  churned only). The review timeline — and each credit's issuing review —
  is on `fact_lifecycle_interval` under `lifecycle_type =
  company_lifecycle`, states `reviewed` and `credit_issued`.
- Three quarters of `dim_account_terms` versions change nothing you can
  see: 10,189 of 13,591 rows carry the same seat price, discount, commit
  and allowance as the row before them (the upstream system re-versioned
  the account on an attribute this warehouse does not carry). Counting
  versions does not count amendments; `LAG` over the four terms columns
  does. The as-of-month-end rule is unaffected.
- There is no churn column on the account or its terms. Churn is the
  `dim_company` version whose `status` is `churned`, one hop away through
  `dim_account.company_id`; its `valid_from` is the cancellation date.
- `dim_user.account_id` points at the **account** (`ACC-`); `dim_account.
  company_id` and `fact_credit.company_id` point at the **company**
  (`CMP-`). Both are "the company" in plain English. A credit reaches its
  account through `dim_account.company_id`, not the other way round.
- `Workflow` (`SKU-007`) launched mid-run and has no `dim_rate_tier` rows.
  An inner join to the ladder silently drops every Workflow session.
- The Analytics band-2 repricing is a **new row** in `dim_rate_tier` with
  its own `effective_from`, not a change to the old one — filter by
  effective window or you will double-count the band.
- `custom_tier.account_id` is written in the same identity surface the
  warehouse elects for accounts (`ACC-#####`). Nothing checks this: a
  ladder row for an id that does not exist joins to zero rows and is never
  reported.
- `dim_date` spans exactly the five run years, 2026-01-01 to 2030-12-31. A
  date the query computes — a credit's expiry, a term end a year out — can
  fall past it and then has no `date_key`; compare such dates as dates or
  instants rather than keying into the calendar.
  `fact_lifecycle_interval.ended_date_key` is NULL on an interval still open
  at the extract's end, like `ended_at`.
- `dim_account.size_band` is the customer's **headcount** band (employees),
  not its seat count. Seats are read off `dim_user_status`; a segmentation
  by `size_band` will not track billable seats and is not meant to.

A B2B SaaS vendor over five years, emitted as billing inputs rather than invoices: companies sign up on a plan tier and provision a hub of seats, their users cycle weekly between using the product and sitting idle, and a repeating renewal review renegotiates the account's seat price, usage discount, committed spend and allowance — or ends it — while a follow-up review sometimes issues a credit. The rate card (plan terms, graduated per-SKU tiers, flat add-ons) and the dated free-usage promotions are data the invoice query joins to; no billed amount is ever stored.

## Reading this export

This is a **dimensional** (star-schema) export: one or more fact tables at a
declared grain, surrounded by dimension tables the facts key into. Every
dimension and fact table below traces to the base-layer emit this export was
built from — nothing here is fabricated.

**Dimensions** carry one row per entity (a `record_id`-keyed identity, unless
key election renders a different surface) plus its projected attributes.
A dimension declared `scd: type2` instead carries **one row per version**:
its history-tracked columns change value across versions, and two columns —
`valid_from` / `valid_to` — bound the sim-time interval each row is current
for. `valid_to` is `NULL` on a version's current (most recent) row; joining a
fact's timestamp against `[valid_from, valid_to)` reconstructs the dimension's
state as of that fact, the standard SCD-2 point-in-time join. A `scd: type1`
dimension carries the current value only, no version rows, no validity
columns.

**Fact tables** carry one row per event at the table's declared grain, with
foreign keys into the surrounding dimensions and any declared measure
columns. A fact's foreign key into an SCD-2 dimension is the dimension's
surrogate key as of the fact's own timestamp — resolved once at build time,
not re-derived by the consumer.

To query this warehouse: join facts to dimensions on the declared key
columns; for an SCD-2 dimension, either join on its current-row surrogate key
(if you only need current attributes) or perform the validity-interval join
above (if you need attribute-as-of-the-fact).

### dim_company

One row per subscribing company, the account holder whose lifecycle runs from onboarding through repeated renewal reviews to churn.

Columns:

- `company_id` (VARCHAR): Unique company identifier (CMP-#####).
- `status` (VARCHAR): CRM lifecycle status: onboarding / active / churned. Renewal reviews do not change it; they are on fact_lifecycle_interval.
- `created_at` (TIMESTAMP): When the company signed up.
- `valid_from` (DATE): Date this version of the row took effect.
- `valid_to` (DATE): Date this version was superseded; NULL on the current version.

Row count: 1368

### dim_account

One row per account, the billing tenant a company provisions — its plan, identity, firmographics, headquarters and billing contact, flat add-ons, and the company it belongs to. The tracked commercial terms live on dim_account_terms.

Columns:

- `account_id` (VARCHAR): Unique account identifier (ACC-#####).
- `company_id` (VARCHAR): dim_company.company_id of the CRM company this account belongs to.
- `name` (VARCHAR): Account display name.
- `domain` (VARCHAR): Company account's web domain.
- `industry` (VARCHAR): Industry sector of the customer organisation.
- `size_band` (VARCHAR): Headcount band of the customer organisation (employees, not seats); it is independent of how many seats the account holds.
- `hq_city` (VARCHAR): Headquarters location of the customer organisation.
- `hq_subdivision` (VARCHAR): Headquarters location of the customer organisation.
- `hq_postal_code` (VARCHAR): Headquarters location of the customer organisation.
- `hq_area_code` (VARCHAR): Headquarters location of the customer organisation.
- `billing_street` (VARCHAR): Street line of the account's billing address.
- `billing_phone` (VARCHAR): Billing contact phone number for the account.
- `plan` (VARCHAR): Plan tier: starter / professional / enterprise; joins to dim_plan_terms.tier.
- `addon_sso` (VARCHAR): 'yes' when the account subscribes to the flat-billed sso SKU, else 'no'.
- `addon_support` (VARCHAR): 'yes' when the account subscribes to the flat-billed premium_support SKU, else 'no'.
- `created_at` (TIMESTAMP): When the account was provisioned; the free trial runs from here for dim_plan_terms.trial_days.
- `created_date_key` (INTEGER): dim_date.date_key of the day the account was provisioned; its month is the account's first billing month.

Declared values:

- `plan`:
  - `starter`
  - `professional`
  - `enterprise`
- `addon_sso`:
  - `no`
  - `yes`
- `addon_support`:
  - `no`
  - `yes`

Row count: 541

### dim_account_terms

One row per version of an account's commercial terms — seat price, negotiated usage discount, committed spend, and allowance override — bounded by the interval each version was current for. Each version is an amendment made at a renewal review.

Columns:

- `account_id` (VARCHAR): dim_account.account_id of the account these terms belong to.
- `seat_price` (BIGINT): Per-seat price at creation, with an SCD-2 trail of the ~10% uplift each renewal renegotiation redraws.
- `pct_of_list_rate` (BIGINT): Percent of list this account pays for rated usage; 100 until an enterprise renewal negotiates it down.
- `term_commit` (BIGINT): Usage spend (USD) the account committed to for the review term that opened with this version; 0 means no commitment. Consecutive versions whose term_commit changed bound one term. [USD]
- `allowance_override` (BIGINT): Negotiated included Core units per seat per month; NULL until an enterprise review sets it. Overrides dim_plan_terms.included_units_per_seat when present.
- `valid_from` (TIMESTAMP): Instant this version of the row took effect.
- `valid_to` (TIMESTAMP): Instant this version was superseded; NULL on the current version.

Row count: 13591

### dim_plan_terms

One row per plan tier's published commercial terms — list seat price, included Core units per seat per month, free-trial length, and credit validity. Joined from dim_account.plan.

Columns:

- `plan_terms_id` (VARCHAR): Unique plan-terms identifier (PLN-##).
- `tier` (VARCHAR): The plan tier this row governs; joins to dim_account.plan.
- `list_seat_price` (BIGINT): Published per-seat monthly price for the tier, before any account's negotiated seat_price. [USD]
- `included_units_per_seat` (BIGINT): Core usage units included per billable seat per month; only volume above seats x this allowance is rated.
- `trial_days` (BIGINT): Days after hub provisioning during which no usage on the account is rated. [days]
- `credit_validity_days` (BIGINT): How long an issued credit can be applied before it expires unused. [days]

Row count: 3

### dim_sku

One row per product line in the catalogue — metered SKUs rated per unit of usage volume through the rate ladder, flat SKUs billed a fixed monthly fee.

Columns:

- `sku_id` (VARCHAR): Unique SKU identifier (SKU-###).
- `name` (VARCHAR): SKU display name.
- `billing_model` (VARCHAR): How the SKU is charged — metered (rated per unit of usage volume through rate_tier) or flat (a fixed monthly fee, never selected by a usage session).
- `standard_rate` (DOUBLE): List price per unit of usage volume; the rate a metered SKU is billed at when it has no ladder rows (Workflow), and the base a custom_tier pct_of_list applies to.
- `flat_fee` (BIGINT): Monthly fee for a flat-billed SKU; absent on metered SKUs, where the charge comes from rate_tier instead. [USD]
- `launched_at` (TIMESTAMP): When the SKU entered the catalogue; Workflow launched mid-run and has no usage before this.

Row count: 7

### dim_rate_tier

One row per band of a metered SKU's graduated price ladder — cumulative monthly volume bounds and the per-unit rate inside the band. A repriced band is a new row with its own effective window, never an edit of the old one.

Columns:

- `rate_tier_id` (VARCHAR): Unique band identifier (RT-###).
- `sku_id` (VARCHAR): dim_sku.sku_id of the SKU this band prices.
- `ordinal` (BIGINT): Position of the band within its SKU's ladder, from 1 upward.
- `from_units` (BIGINT): Inclusive lower bound of cumulative period volume this band applies to.
- `to_units` (BIGINT): Inclusive upper bound of the band; NULL on the open-ended top band.
- `rate` (DOUBLE): Price per unit (USD) inside this band, before the account's negotiated pct_of_list_rate. [USD]
- `effective_from` (TIMESTAMP): Instant this band came into force.
- `effective_to` (TIMESTAMP): Instant this band was withdrawn; NULL while still in force.

Row count: 10

### dim_promotion

One row per dated campaign that waives rating on one SKU while active — the billing-side record of each spring push.

Columns:

- `promotion_id` (VARCHAR): Unique promotion identifier (PRM-###).
- `sku_id` (VARCHAR): dim_sku.sku_id of the SKU whose usage the promotion waives.
- `name` (VARCHAR): Promotion display name.
- `kind` (VARCHAR): What the promotion grants; free_usage waives rating on the SKU between the record's creation and deactivation times.
- `starts_at` (TIMESTAMP): Instant the promotion became active.
- `ends_at` (TIMESTAMP): Instant the promotion ended; NULL if still active at the end of the extract.

Row count: 5

### fact_credit

One row per one-time credit issued to a company after a renewal review — face value and reason. Whether and when it is consumed against invoices is derived by the billing query, never stored.

Columns:

- `credit_id` (VARCHAR): Unique credit identifier (CRD-#####).
- `company_id` (VARCHAR): dim_company.company_id of the company the credit was issued to; reach its account through dim_account.company_id.
- `amount` (BIGINT): Face value of the credit (USD) at issue. [USD]
- `reason` (VARCHAR): Why the credit was issued — goodwill, sla_breach, or billing_error.
- `issued_at` (TIMESTAMP): When the credit was issued; it expires issued_at + dim_plan_terms.credit_validity_days later.
- `issued_date_key` (INTEGER): dim_date.date_key of the issue day; the credit enters the ledger in that month.

Declared values:

- `reason`:
  - `goodwill`
  - `sla_breach`
  - `billing_error`

Row count: 447

### dim_user

One row per seat holder, an individual working inside a subscribing company's account.

Columns:

- `user_id` (VARCHAR): Unique user identifier (USR-######).
- `account_id` (VARCHAR): dim_account.account_id of the account this user's seat belongs to.
- `first_name` (VARCHAR): User's given name.
- `last_name` (VARCHAR): User's family name.
- `email` (VARCHAR): User's work email address on their company's domain, following the company's email convention.
- `job_title` (VARCHAR): User's job title as recorded on the seat.
- `phone` (VARCHAR): User's direct work phone number.
- `created_at` (TIMESTAMP): When the user's seat was created.

Row count: 9858

### dim_user_status

One row per seat's status interval, tracking a user's lifecycle from provisioned through active/using to offboarded; billable seat-days are the active/using intervals.

Columns:

- `user_id` (VARCHAR): Unique user identifier (USR-######).
- `status` (VARCHAR): Seat lifecycle status: provisioned / active / using / offboarded.
- `valid_from` (DATE): Date this version of the row took effect.
- `valid_to` (DATE): Date this version was superseded; NULL on the current version.

Row count: 24599

### fact_usage_event

One row per product usage session — the volume a seat holder ran, attributed to the SKU the session bound.

Columns:

- `usage_event_id` (BIGINT): Sequential event id (0-based), ascending in event order.
- `occurred_at` (TIMESTAMP): When the usage session occurred.
- `occurred_date_key` (INTEGER): dim_date.date_key of the session's day; usage is billed in that month.
- `user_id` (VARCHAR): dim_user.user_id of the user who ran the session.
- `sku_id` (VARCHAR): dim_sku.sku_id of the product line the session was recorded against.
- `context` (VARCHAR): JSON detail of the usage session; its `volume` leaf is the metered quantity in the SKU's units, rounded to 2dp.

Row count: 588840

### fact_lifecycle_interval

One row per state interval occupied by a company or user lifecycle.

Columns:

- `lifecycle_id` (VARCHAR): Id of the lifecycle this interval belongs to; rows sharing an id form one company or user lifecycle timeline.
- `lifecycle_type` (VARCHAR): Which lifecycle this is: company_lifecycle or user_lifecycle.
- `company_id` (VARCHAR): dim_company.company_id for company_lifecycle rows; NULL on user_lifecycle rows.
- `user_id` (VARCHAR): dim_user.user_id for user_lifecycle rows; NULL on company_lifecycle rows.
- `state` (VARCHAR): Lifecycle state occupied during this interval.
- `started_at` (TIMESTAMP): When the lifecycle entered this state.
- `ended_at` (TIMESTAMP): When the lifecycle left this state; NULL if it was still in this state at the end of the extract.
- `started_date_key` (INTEGER): dim_date.date_key of the day the state was entered.
- `ended_date_key` (INTEGER): dim_date.date_key of the day the state was left; NULL while still occupied.

Declared values:

- `lifecycle_type`:
  - `company_lifecycle`: The account lifecycle — a company onboards onto a freshly provisioned hub, then enters a repeating renewal review that each quarter either renegotiates its commercial terms or ends the subscription, marking the hub so its seats offboard in turn.
  - `user_lifecycle`: The seat lifecycle — a user is provisioned against its company's hub, then cycles week by week between using the product and letting the cycle pass, until the account churns and the seat is given up.

Row count: 1210022

### dim_date

A forge-generated calendar over the declared range, one row per day, keyed by `yyyymmdd`.

Columns:

- `date_key` (INTEGER): `year * 10000 + month * 100 + day` — the `yyyymmdd` smart key; the table's row identity and sort order
- `date` (DATE): The calendar date
- `year` (INTEGER): Calendar year
- `quarter` (INTEGER): 1–4
- `month` (INTEGER): 1–12
- `day` (INTEGER): Day of month, 1–31
- `day_of_week` (INTEGER): ISO weekday, 1 = Monday … 7 = Sunday
- `day_of_year` (INTEGER): 1–366
- `iso_year` (INTEGER): ISO-8601 week-numbering year (differs from `year` around the new year)
- `iso_week` (INTEGER): ISO-8601 week number, 1–53, within `iso_year`
- `month_name` (VARCHAR): English month name (`January` … `December`)
- `day_name` (VARCHAR): English weekday name (`Monday` … `Sunday`)
- `is_weekend` (BOOLEAN): `day_of_week >= 6`

Row count: 1826

### custom_tier

The side table finance hands over with the words "this is the contract
now" — not produced by the simulation, and carrying no foreign-key
guarantee.

**Override rule.** For an account + SKU + month, if `custom_tier` has rows
whose `[effective_from, effective_to]` window contains the month end, the
tiered-rating stage uses those rows and ignores `dim_rate_tier` for that
SKU. SKUs the account has no custom rows for fall back to `dim_rate_tier`
as normal. `effective_to` is inclusive; empty means still in force.

**Two pricing forms.** A row carries exactly one of `rate` (a per-unit
price, replaces the band's `dim_rate_tier.rate`) or `pct_of_list` (a
percentage applied to `dim_sku.standard_rate` for volume in that band,
replacing the account's `pct_of_list_rate` — not stacked with it).

**Selection.** The four accounts are the top enterprise accounts by total
usage volume over the run (`ACC-00002`, `ACC-00095`, `ACC-00027`,
`ACC-00096`); their ladders were authored by hand, with each
`effective_from` on one of that account's real renewal-review dates.

**Irregularities to expect.**

- `ACC-00002` has two Core ladders in sequence — five bands until
  2028-05-24, four bands from 2028-05-25 — with `ordinal` restarting at 1 in
  the second window. Its Analytics and API ladders have a single open
  window; Storage and Workflow have no custom rows.
- `ACC-00095`'s ladder starts mid-life (2027-07-04); earlier months rate on
  `dim_rate_tier`.
- `ACC-00027`'s Analytics ladder has a **coverage gap**: no band covers
  10,001–12,000 units. The spec does not say what happens to volume in the
  gap; the reference query must choose (and document) a rule.
- `ACC-00096`'s Core ladder is in `pct_of_list` form, and the account also
  carries its own negotiated `pct_of_list_rate` on `dim_account_terms`.
- `ACC-00027` and `ACC-00096` churned; their windows close on the churn
  date. `ACC-00002` and `ACC-00095` are open at the end of the extract.

Negotiated per-account price ladders for the largest enterprise accounts, maintained by account management outside the product. Rows here override dim_rate_tier for that account and SKU while effective; forge carries them verbatim and enforces no reference.

Columns:

- `account_id` (VARCHAR): dim_account.account_id the ladder belongs to (ACC-#####); not checked against the warehouse.
- `sku_id` (VARCHAR): dim_sku.sku_id the ladder prices (SKU-###); not checked against the warehouse.
- `ordinal` (BIGINT): Position of the band within its ladder, from 1 upward; restarts at 1 in each effective window.
- `from_units` (BIGINT): Inclusive lower bound of cumulative monthly volume the band applies to.
- `to_units` (BIGINT): Inclusive upper bound of the band; empty on the open-ended top band.
- `rate` (DECIMAL(6,4)): Negotiated price per unit (USD) inside the band; empty when the row is priced as pct_of_list instead.
- `pct_of_list` (BIGINT): Percent of dim_sku.standard_rate charged inside the band, replacing the account's pct_of_list_rate; empty when the row carries a rate.
- `effective_from` (DATE): First day the ladder applies, one of the account's renewal-review dates.
- `effective_to` (DATE): Last day the ladder applies (inclusive); empty while still in force.

Row count: 28

## Anchor

- Start instant: 2026-01-01T00:00:00+00:00
- Timezone: UTC

## Emit Identity

- base_format_version: 11
- base.json SHA-256: dc745d7ba69e12c178707a6e9e2647605f71707b3aeb3b7cef8780a319176b13
- fork_path: trunk
- slice_at: 157680000000000000
- runtime: timezone=UTC, start_datetime=2026-01-01T00:00:00+00:00
